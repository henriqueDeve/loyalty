quarkus:
  flyway:
    migrate-at-start: true

"%dev":
  "datasource":
    db-kind: postgresql
    username: admin
    password: admin
    reactive:
      url: jdbc:postgresql://localhost:5432/devdb
      schema: public
      initialization-mode: always
      init-script-paths: db/migration


"%prod":
  datasource:
    db-kind: postgresql
    username: seuusuario_prod
    password: suasenha_prod
    reactive:
      url: jdbc:postgresql://localhost:5432/seubanco_prod

